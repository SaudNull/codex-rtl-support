# Contributing

Bug reports and focused pull requests are welcome.

Before opening a PR:

```bash
npm install
npm test
npm run dry-run
```

Keep changes small and avoid unrelated formatting churn. RTL fixes should preserve LTR behavior for code blocks, diffs, terminals, and file paths.
